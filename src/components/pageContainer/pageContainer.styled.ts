import styled from '@emotion/styled'

export const Container = styled.div`
  padding-top: 1.5rem;
  padding-left: 1.5rem;
  padding-right: 1.5rem;
  isolation: isolate;
  display: flex;
  flex-flow: column;
  gap: 1.5rem;
  flex: 1 1 auto;
  max-width: 90rem;
  width: 100vw;
  margin: auto;
`
